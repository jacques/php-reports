parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpunit:
            config_file: tests/phpunit.xml
            testsuite: ~
            group: []
            always_execute: false
        phpcs:
            standard: PSR2
            warning_severity: ~
            ignore_patterns:
              - tests/
            triggered_by: [php]
